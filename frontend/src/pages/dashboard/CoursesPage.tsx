import { useState } from 'react'
import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query'

import { createCourse, createCourseCategory, deleteCourse, deleteCourseCategory, fetchCourseCategories, fetchCourseInstructors, fetchCourses, updateCourse, updateCourseCategory, type CourseRecord } from '../../api/management'

type CourseForm = { title: string; description: string; category_id: string; instructor_id: string; status: CourseRecord['status']; level: CourseRecord['level']; max_students: string; price: string; thumbnail_url: string; course_type: CourseRecord['course_type'] }
const emptyForm: CourseForm = { title: '', description: '', category_id: '', instructor_id: '', status: 'draft', level: 'beginner', max_students: '', price: '0', thumbnail_url: '', course_type: 'technical' }
const statusLabels = { draft: 'مسودة', published: 'منشور', archived: 'مؤرشف' }
const levelLabels = { beginner: 'مبتدئ', intermediate: 'متوسط', advanced: 'متقدم' }

export default function CoursesPage() {
  const queryClient = useQueryClient()
  const [search, setSearch] = useState('')
  const [formOpen, setFormOpen] = useState(false)
  const [editing, setEditing] = useState<CourseRecord | null>(null)
  const [form, setForm] = useState<CourseForm>(emptyForm)
  const [categoryName, setCategoryName] = useState('')
  const [categoryDescription, setCategoryDescription] = useState('')
  const [editingCategory, setEditingCategory] = useState<number | null>(null)
  const coursesQuery = useQuery({ queryKey: ['courses'], queryFn: fetchCourses })
  const categoriesQuery = useQuery({ queryKey: ['course-categories'], queryFn: fetchCourseCategories })
  const instructorsQuery = useQuery({ queryKey: ['course-instructors', form.category_id], queryFn: () => fetchCourseInstructors(Number(form.category_id)), enabled: Boolean(form.category_id) })
  const closeForm = () => { setFormOpen(false); setEditing(null); setForm(emptyForm) }
  const saveMutation = useMutation({ mutationFn: () => editing ? updateCourse(editing.id, { ...form, category_id: Number(form.category_id), instructor_id: Number(form.instructor_id), max_students: form.max_students ? Number(form.max_students) : null, price: Number(form.price) }) : createCourse({ ...form, category_id: Number(form.category_id), instructor_id: Number(form.instructor_id), max_students: form.max_students ? Number(form.max_students) : null, price: Number(form.price) }), onSuccess: () => { closeForm(); queryClient.invalidateQueries({ queryKey: ['courses'] }) } })
  const deleteMutation = useMutation({ mutationFn: deleteCourse, onSuccess: () => queryClient.invalidateQueries({ queryKey: ['courses'] }) })
  const categoryMutation = useMutation({ mutationFn: () => editingCategory ? updateCourseCategory(editingCategory, { name: categoryName, description: categoryDescription }) : createCourseCategory({ name: categoryName, description: categoryDescription }), onSuccess: () => { setCategoryName(''); setCategoryDescription(''); setEditingCategory(null); queryClient.invalidateQueries({ queryKey: ['course-categories'] }) } })
  const deleteCategoryMutation = useMutation({ mutationFn: deleteCourseCategory, onSuccess: () => queryClient.invalidateQueries({ queryKey: ['course-categories'] }) })
  const updateField = (field: keyof CourseForm, value: string) => setForm((current) => ({ ...current, [field]: value }))
  const openCreate = () => { setEditing(null); setForm(emptyForm); setFormOpen(true) }
  const openEdit = (course: CourseRecord) => { setEditing(course); setForm({ title: course.title, description: course.description ?? '', category_id: String(course.category_id), instructor_id: String(course.instructor_id), status: course.status, level: course.level, max_students: course.max_students ? String(course.max_students) : '', price: String(course.price), thumbnail_url: course.thumbnail_url ?? '', course_type: course.course_type }); setFormOpen(true) }
  const courses = (coursesQuery.data?.data ?? []).filter((course) => `${course.title} ${course.category?.name ?? ''} ${course.instructor?.name ?? ''}`.toLowerCase().includes(search.toLowerCase()))

  if (coursesQuery.isLoading || categoriesQuery.isLoading || (formOpen && Boolean(form.category_id) && instructorsQuery.isLoading)) return <div className="py-16 text-center text-slate-500">جاري تحميل الكورسات...</div>
  if (coursesQuery.isError || categoriesQuery.isError || instructorsQuery.isError) return <div className="py-16 text-center text-rose-600">تعذر تحميل بيانات الكورسات</div>
  const categories = categoriesQuery.data ?? []
  const instructors = instructorsQuery.data ?? []

  return <div className="space-y-6">
    <header className="flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="text-sm font-semibold text-amber-600">إدارة المحتوى</p><h1 className="mt-1 text-3xl font-bold text-slate-900">الكورسات</h1><p className="mt-2 text-slate-500">أنشئ برامج تعليمية، حدّد المدرسين وتابع التسجيلات</p></div><button type="button" onClick={openCreate} className="rounded-xl bg-amber-500 px-5 py-3 font-semibold text-white hover:bg-amber-600">+ كورس جديد</button></header>
    <section className="flex flex-col gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:flex-row sm:items-center sm:justify-between"><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="ابحث باسم الكورس أو الفئة أو المدرس" className="w-full rounded-xl border border-slate-200 px-4 py-3 outline-none focus:border-amber-500 sm:max-w-md" /><span className="text-sm text-slate-500">{courses.length} كورس</span></section>
    {formOpen && <form onSubmit={(event) => { event.preventDefault(); saveMutation.mutate() }} className="grid gap-4 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm md:grid-cols-2"><h2 className="text-lg font-bold text-slate-900 md:col-span-2">{editing ? 'تعديل الكورس' : 'إنشاء كورس جديد'}</h2><input required value={form.title} onChange={(event) => updateField('title', event.target.value)} placeholder="اسم الكورس" className="rounded-xl border border-slate-200 px-4 py-3 md:col-span-2" /><textarea value={form.description} onChange={(event) => updateField('description', event.target.value)} placeholder="وصف الكورس" className="min-h-28 rounded-xl border border-slate-200 px-4 py-3 md:col-span-2" /><select required value={form.category_id} onChange={(event) => { updateField('category_id', event.target.value); updateField('instructor_id', '') }} className="rounded-xl border border-slate-200 px-4 py-3"><option value="">اختر الفئة</option>{categories.map((category) => <option key={category.id} value={category.id}>{category.name}</option>)}</select><select required value={form.instructor_id} onChange={(event) => updateField('instructor_id', event.target.value)} disabled={!form.category_id} className="rounded-xl border border-slate-200 px-4 py-3 disabled:bg-slate-100"><option value="">{form.category_id ? 'اختر المدرس المطابق للفئة' : 'اختر الفئة أولاً'}</option>{(instructorsQuery.data ?? []).map((instructor) => <option key={instructor.id} value={instructor.id}>{instructor.name}</option>)}</select><select value={form.course_type} onChange={(event) => updateField('course_type', event.target.value)} className="rounded-xl border border-slate-200 px-4 py-3"><option value="technical">تقني</option><option value="craft">حرف</option></select><select value={form.level} onChange={(event) => updateField('level', event.target.value)} className="rounded-xl border border-slate-200 px-4 py-3"><option value="beginner">مبتدئ</option><option value="intermediate">متوسط</option><option value="advanced">متقدم</option></select><input type="number" min="1" value={form.max_students} onChange={(event) => updateField('max_students', event.target.value)} placeholder="الحد الأقصى للطلاب" className="rounded-xl border border-slate-200 px-4 py-3" /><input type="number" min="0" step="0.01" value={form.price} onChange={(event) => updateField('price', event.target.value)} placeholder="السعر" className="rounded-xl border border-slate-200 px-4 py-3" /><input type="url" value={form.thumbnail_url} onChange={(event) => updateField('thumbnail_url', event.target.value)} placeholder="رابط الصورة المصغرة" className="rounded-xl border border-slate-200 px-4 py-3 md:col-span-2" />{editing && <select value={form.status} onChange={(event) => updateField('status', event.target.value)} className="rounded-xl border border-slate-200 px-4 py-3"><option value="draft">مسودة</option><option value="published">منشور</option><option value="archived">مؤرشف</option></select>}<div className="flex gap-3 md:col-span-2"><button disabled={saveMutation.isPending} className="rounded-xl bg-amber-500 px-5 py-3 font-semibold text-white disabled:opacity-50">{saveMutation.isPending ? 'جاري الحفظ...' : 'حفظ الكورس'}</button><button type="button" onClick={closeForm} className="rounded-xl border border-slate-200 px-5 py-3">إلغاء</button></div>{saveMutation.isError && <p className="text-rose-600 md:col-span-2">تعذر حفظ الكورس، تحقق من البيانات.</p>}</form>}
    <section className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="w-full min-w-[1050px] text-right"><thead className="bg-slate-50 text-sm text-slate-500"><tr>{['الكورس', 'الفئة والنوع', 'المدرس', 'الحالة', 'التسجيلات', 'السعر', 'الإجراءات'].map((heading) => <th key={heading} className="px-5 py-4 font-semibold">{heading}</th>)}</tr></thead><tbody>{courses.map((course) => <tr key={course.id} className="border-t border-slate-100 hover:bg-slate-50"><td className="px-5 py-4"><div className="flex items-center gap-3">{course.thumbnail_url ? <img src={course.thumbnail_url} alt="" className="h-12 w-16 rounded-lg object-cover" /> : <div className="h-12 w-16 rounded-lg bg-amber-100" />}<div><p className="font-semibold text-slate-900">{course.title}</p><p className="text-xs text-slate-500">{levelLabels[course.level]}</p></div></div></td><td className="px-5 py-4"><p className="text-slate-700">{course.category?.name}</p><p className="text-xs text-slate-500">{course.course_type === 'technical' ? 'تقني' : 'حرف'}</p></td><td className="px-5 py-4 text-slate-700">{course.instructor?.name ?? 'غير محدد'}</td><td className="px-5 py-4"><span className={`rounded-full px-3 py-1 text-xs font-semibold ${course.status === 'published' ? 'bg-emerald-100 text-emerald-700' : course.status === 'archived' ? 'bg-slate-200 text-slate-600' : 'bg-amber-100 text-amber-700'}`}>{statusLabels[course.status]}</span></td><td className="px-5 py-4 text-slate-700">{course.enrollments_count ?? 0}{course.max_students ? ` / ${course.max_students}` : ''}</td><td className="px-5 py-4 font-semibold text-slate-800">{Number(course.price).toFixed(2)} ر.س</td><td className="px-5 py-4"><button onClick={() => openEdit(course)} className="ml-3 text-blue-600">تعديل</button><button disabled={deleteMutation.isPending} onClick={() => confirm(`حذف ${course.title}؟`) && deleteMutation.mutate(course.id)} className="text-rose-600 disabled:opacity-50">حذف</button></td></tr>)}</tbody></table></div>{courses.length === 0 && <p className="p-12 text-center text-slate-500">لا توجد كورسات</p>}</section>
    <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="mb-4 flex items-center justify-between"><div><h2 className="text-lg font-bold text-slate-900">تصنيفات الكورسات</h2><p className="text-sm text-slate-500">تستخدم لتحديد المدرسين المتاحين لكل كورس</p></div><span className="text-sm text-slate-500">{categories.length} تصنيف</span></div><form onSubmit={(event) => { event.preventDefault(); categoryMutation.mutate() }} className="mb-5 grid gap-3 md:grid-cols-[1fr_2fr_auto]"><input required value={categoryName} onChange={(event) => setCategoryName(event.target.value)} placeholder="اسم التصنيف" className="rounded-xl border border-slate-200 px-4 py-3" /><input value={categoryDescription} onChange={(event) => setCategoryDescription(event.target.value)} placeholder="وصف التصنيف" className="rounded-xl border border-slate-200 px-4 py-3" /><button disabled={categoryMutation.isPending} className="rounded-xl bg-slate-900 px-5 py-3 font-semibold text-white">{editingCategory ? 'تحديث' : 'إضافة'}</button></form><div className="grid gap-3 md:grid-cols-3">{categories.map((category) => <div key={category.id} className="rounded-xl border border-slate-100 bg-slate-50 p-4"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold text-slate-900">{category.name}</p><p className="mt-1 text-xs text-slate-500">{category.courses_count} كورس</p></div><div className="flex gap-2 text-sm"><button onClick={() => { setEditingCategory(category.id); setCategoryName(category.name); setCategoryDescription(category.description ?? '') }} className="text-blue-600">تعديل</button><button onClick={() => confirm(`حذف تصنيف ${category.name}؟`) && deleteCategoryMutation.mutate(category.id)} className="text-rose-600">حذف</button></div></div></div>)}</div></section>
  </div>
}